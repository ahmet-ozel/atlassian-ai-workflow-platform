# Temporal dynamic config (development, SQL persistence)
# Minimal valid YAML for local Temporal startup.
# Real production tuning belongs in production configuration; this file only ensures
# Temporal starts cleanly in dev mode.
frontend.enableClientVersionCheck:
  - value: false
    constraints: {}
