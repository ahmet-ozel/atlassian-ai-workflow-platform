global:
  resolve_timeout: 5m

route:
  receiver: platform-null
  group_by:
    - alertname
    - service
    - job
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: platform-null
