# Vault (dev mode only)

Dev-mode Vault only (`-dev` flag); no production HA/KMS configuration here.

This directory is a placeholder for development-only Vault assets. Production HA
topology, KMS auto-unseal, and real mTLS material are explicitly **out of scope**
for this development setup and will be addressed in a future ops/security pass.
