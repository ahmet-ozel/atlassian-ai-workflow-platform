# =============================================================================
# opencode-sidecar .env.example
# =============================================================================
# Compose-internal LLM code-generation sidecar.
# Reachable only on the Compose internal network via ``expose: 4096`` —
# this service MUST NOT publish any host port (test_compose_structure
# enforces ``ports:`` absence for ``kind=sidecar``).
#
# This file is the per-service slice of the master env reference table
# (``platform/docs/env-reference.md``) and stays in parity
# with it (verified by
# ``tests/property/test_env_coverage.py``).
#
# All values are PLACEHOLDERS / DEV-ONLY. Never commit a populated
# ``.env`` to git.
# =============================================================================

# -----------------------------------------------------------------------------
# Service runtime
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# LLM upstream (env-reference §5 — Compose stack does NOT package vLLM,
# The endpoint is resolved from VLLM_BASE_URL.
# -----------------------------------------------------------------------------
LLM_PROVIDER=vllm
LLM_MODEL_NAME=qwen2.5-coder
OPENAI_BASE_URL=https://api.openai.com/v1
# vault_path: vault:infrastructure/openai/api_key
OPENAI_API_KEY=
VLLM_BASE_URL=http://host.docker.internal:8000/v1
# For local vLLM without auth, enter not-needed explicitly in the dashboard.
# vault_path: vault:infrastructure/vllm/api_key
VLLM_API_KEY=
ANTHROPIC_BASE_URL=https://api.anthropic.com/v1
# vault_path: vault:infrastructure/anthropic/api_key
ANTHROPIC_API_KEY=
