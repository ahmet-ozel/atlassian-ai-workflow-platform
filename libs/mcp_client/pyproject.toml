[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "mcp_client"
version = "0.0.0"
description = "MCP client library with banned tool filtering and PR draft enforcement."
readme = "README.md"
requires-python = ">=3.12,<3.13"
license = { text = "Proprietary" }
authors = [{ name = "Platform" }]
dependencies = []

[project.optional-dependencies]
audit = ["audit-logger"]

[tool.hatch.build.targets.wheel]
packages = ["src/mcp_client"]

[tool.hatch.build.targets.sdist]
include = [
    "src/mcp_client",
    "README.md",
    "pyproject.toml",
]
